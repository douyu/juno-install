
name: docker-compose-actions-workflow
on: push
jobs:
  check-docker-compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: start juno assembly and juno service
        run: docker-compose up -d --build
      - name: docker ps
        run: docker ps
      - name: curl test
        run: curl 127.0.0.1:50002
